#include "main.h"

/**
 * _abs - Calculate the absolute value of n.
 *
 * @n: An integer.
 *
 * Return: Absolute value of n.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
